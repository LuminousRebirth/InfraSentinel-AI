# InfraSentinel AI 项目需求文档（第一版）

## 1. 项目定位

**InfraSentinel AI** 是一个部署在 Windows 本地服务器上的企业级视觉智能检测、分析与预警平台。它以现有 YOLO26 管道缺陷与安全帽识别 Demo 为推理基础，提供 Web 管理平台和 Windows 桌面客户端。两端使用同一业务能力；桌面端额外具备本地离线检测与网络恢复后的安全同步能力。

系统服务于巡检人员、安全员和管理员，用于识别管道缺陷及未佩戴安全帽行为，形成“采集/上传 → YOLO 检测 → 规则分级 → LLM 多点分析 → 预警处置 → 报告与追溯”的完整闭环。

首期运行基准为 Windows + NVIDIA RTX 4060 笔记本。系统必须支持简体中文和英文，并采用具有科技感、信息密度适中且可读性优先的界面设计。

## 2. 第一版范围与边界

### 2.1 必须交付

- Web 管理平台和 Windows 桌面客户端。
- 注册、登录、管理员审批；管理员与普通用户两种角色。
- 工作台、智能检测、预警中心、数据集管理、模型管理、用户管理、个人中心和系统设置。
- 管道缺陷与安全帽 YOLO26 模型的图片、视频、OBS 虚拟摄像头实时检测。
- 云端多模态模型（Qwen、DeepSeek、GLM 等）的统一配置、选择和调用。
- PostgreSQL 业务数据库、Milvus 向量数据库预留、Redis 异步任务服务。
- 在线标注、数据集校验、YOLO26 训练、模型评估、模型发布与回滚。
- 预警闭环、审计日志、报告导出、健康监控、LLM 成本控制与证据链。
- Conda 统一开发/运行环境；大型模块完成即通过 GitHub 里程碑版本存档。

### 2.2 明确不在第一版实现

- 多路摄像头并发检测（架构必须能扩展；首期只验证 1 路 OBS 虚拟摄像头）。
- 企业微信、钉钉、短信、邮件等外部预警推送。
- 定时备份与恢复功能。
- Agent/RAG 智能问答与知识检索的实际业务功能。
- YOLO26 之外的 YOLO 主版本训练与推理。

系统仍需为以上能力提供清晰的接口、配置位和数据结构扩展点。第二阶段将基于 PostgreSQL + Milvus 上线 Agent/RAG。

## 3. 角色、权限与账号生命周期

| 角色 | 主要权限 |
|---|---|
| 普通用户 | 自助注册；提交/查看本人检测任务、检测记录和预警；处置被分派预警；配置个人 API Key；使用桌面端离线检测与同步。 |
| 管理员 | 审批注册账号；管理用户、项目/点位、类别、数据集、模型、LLM 配置、脱敏策略、预警规则和系统设置；查看全量数据、审计日志、健康状态及成本统计。 |

账号状态至少包含“待审核、已启用、已停用、已拒绝”。访客注册成功后无法使用系统，直至管理员审核通过。密码必须使用安全哈希保存；会话、权限校验和敏感配置不得仅依赖前端控制。

## 4. 信息架构与功能需求

### 4.1 工作台（概要）

- 展示图片/视频/实时检测任务量、待处理预警、已发布模型、数据集版本、训练任务和 LLM 调用概览。
- 展示高/中/低风险趋势、类别分布、项目/点位热力图、预警处理时效、近 7/30 天趋势及最新预警事件。
- 提供“待我处理”“任务失败”“模型待评估”“系统资源异常”等快捷入口。
- 按项目、点位、检测类型、时间范围、模型版本筛选全部统计。

### 4.2 智能检测

#### 图片检测

- 支持单张与批量图片上传、格式/大小校验、任务进度和失败重试。
- 可选择已发布模型，设置置信度阈值、IoU 阈值、输入尺寸和推理设备策略；阈值应可保存为预设。
- 输出原图、YOLO 标注图、检测框/类别/置信度/坐标、类别统计、耗时和模型版本。
- LLM 分析可选择自动执行或手动发起；默认自动执行。自动模式可由系统、项目或本次任务覆盖。
- LLM 对每一个检测对象生成风险说明、严重性、可能原因及修复建议；在标注图上点击检测框即可查看该对象的分析结果。还须提供全局风险结论、风险排序、关联判断和优先级处置清单。
- 管理员配置的规则引擎决定预警等级；LLM 只提供分析建议，不得独立决定最终等级。人工可调整最终等级并留下原因。

#### 图片记录

- 保存所有图片检测任务、原图、标注图、检测结果、LLM 结论、规则命中、操作人、模型/阈值和时间。
- 支持条件筛选、全文搜索、批量导出、重新分析、重新检测、查看详情、生成报告和删除（受权限与审计约束）。

#### 视频检测与记录

- 支持常见视频上传、后台异步处理、进度、取消、失败原因和失败重试。
- 可配置模型、置信度、IoU、抽帧/检测 FPS 和视频输出质量。
- 输出可播放、下载的标注视频，逐帧/按时间段统计、缺陷关键帧、关键时间轴、模型版本和处理性能。
- 连续画面中同一缺陷或未戴安全帽行为必须合并为一条事件，包含开始/结束时间、持续时长、最严重帧、最高风险与对应 LLM 分析，避免逐帧重复告警。
- 视频记录与图片记录具有同等检索、详情、重跑、报告和删除能力。

#### 实时检测

- 首期支持 Windows 的 OBS 虚拟摄像头；用户可在 OBS 播放本地视频模拟实时流。
- 展示实时预览、标注框、实时 FPS、检测延迟、模型、阈值、GPU/显存状态和事件列表。
- FPS/抽帧策略必须可动态调整，以在 RTX 4060 上按准确性与流畅度平衡；不得把固定 FPS 作为唯一运行模式。
- 预警事件沿用视频去重合并规则，并可显示关键帧和多点 LLM 分析。
- 桌面端离线时应持续完成本机检测，采用加密的本地待同步队列保存媒体、任务结果、预警与操作记录；连接服务器后显示同步进度、冲突与失败重试。

### 4.3 预警中心

- 集中展示所有预警，支持风险等级、状态、项目/点位、模型、类别、时间、负责人等多条件筛选。
- 默认状态流转：`待确认 → 已分派 → 处理中 → 已解决 / 误报关闭`。
- 每条预警提供规则命中、原始/标注媒体、关键帧、检测与 LLM 详情、模型/阈值、事件时间轴和完整证据链。
- 支持分派处理人、填写处置说明、上传佐证附件、设定响应/关闭时限；超时显示升级状态并在管理员工作台突出展示。
- 支持管理员设置等级规则、冷却时间、去重窗口、静默时段、白名单和项目差异化规则。
- 外部通知通道不在第一版实现；系统内弹窗、声音提示、预警中心和工作台提醒必须实现。

### 4.4 数据集管理与在线标注

- 支持上传原始图片、视频及已有标注的 YOLO 数据集；支持压缩包导入、目录/文件结构预览和格式校验。
- 支持项目化、版本化管理，记录数据来源、创建者、类别、样本数、存储量、训练/验证/测试划分和变更历史。
- 管理员可创建、编辑、停用类别；现有类别为 CK 裂缝、PL 修补、SG 沉积、SL 表面破损、TL 树根、ZW 障碍物、helmet、no_helmet。
- 提供网页内图片框选标注、类别修改、标签删除、审核、撤销/重做及 YOLO 格式导出。
- 支持视频抽帧并对关键帧执行同样的图片标注流程。
- 自动检测空标注、损坏文件、错误/越界框、重复图片、类别失衡、训练/验证集泄漏等数据质量问题，并给出可执行修复建议。

### 4.5 模型管理、训练与评估

- 管理 YOLO26 n/s/m/l/x 规格、导入权重、自训练权重、模型卡、类别映射、创建者、训练数据版本、发布日期、状态和运行指标。
- 支持发起后台训练任务，配置数据集版本、基座权重、训练轮数、批大小、图像尺寸、学习率、增强策略和 GPU 设备；提供队列、日志、进度、取消、失败重试和训练产物管理。
- 展示训练曲线、损失、mAP、Precision、Recall、F1、混淆矩阵、PR 曲线、样例预测和每类评估结果。
- 模型发布前必须通过管理员确认；支持草稿、评估中、已发布、已归档状态，支持灰度启用和回滚到旧版本。
- 模型评估页支持选择带标签测试集，比较多个模型/阈值，并生成评估报告。

### 4.6 LLM 与系统设置

- 提供 Qwen、DeepSeek、GLM 等供应商的可扩展适配层；根据任务类型校验模型是否具备视觉能力。纯文本模型只能用于文本总结、报告润色等非视觉任务。
- 管理员可配置系统级供应商、Endpoint、模型、默认策略、配额、超时、重试和脱敏策略；普通用户可以配置加密保存的个人 API Key，并选择在有权限的范围内优先使用个人或系统配置。
- 支持模型可用性测试、调用日志、成功率、耗时、Token/费用估算、用户/项目/模型维度成本统计、额度限制和服务不可用降级。
- LLM 不可用时，YOLO 检测和规则预警必须正常完成；界面需明确提示“LLM 分析待补充/失败”，允许重试。
- 管理员可选择并管理对发送至云端图片的脱敏策略，例如人脸或指定敏感区域打码。

### 4.7 项目、点位、系统健康与审计

- 项目/点位管理必须包含厂区、区域、管道/资产编号、摄像头/OBS 通道、负责人和启停状态；任务与预警均须归属到项目及点位。
- 健康监控显示 CPU、GPU、显存、磁盘、数据库/Redis/Milvus 可用性、推理 FPS、任务队列、API 调用状态和失败任务。
- 操作审计日志必须记录登录、注册审批、用户/权限变更、数据集/类别/模型变更、检测与报告、预警分派及处置、LLM 与系统配置变更。日志至少记录操作者、时间、来源、目标对象、操作、前后状态和结果。

### 4.8 个人中心与国际化

- 个人资料、修改密码、登录记录、个人 API Key、个人检测偏好、语言设置和桌面端同步状态。
- 所有界面文本、状态、报告模板与错误信息采用可国际化资源；第一版提供简体中文和英文。

### 4.9 报告和证据留存

- 图片、视频、实时事件及预警处置均可生成 PDF、Excel、Markdown 报告。
- 导出时可选择时间范围、项目/点位、风险级别、原始/标注媒体、检测明细、LLM 分析、处置过程和审计信息。
- 原始图片/视频、标注文件和报告默认长期保留；授权用户后续可手动删除，删除动作必须写入审计日志。

## 5. 技术与部署约束

- **推理底座**：沿用现有 Python 3.11、YOLO26、Ultralytics、TensorRT/ONNX 优先、PyTorch 回退的能力；不得破坏既有 CLI 与 Demo 基本推理行为。
- **服务端**：Python + FastAPI，按领域模块组织 API、业务服务、数据访问、异步任务与推理适配层。
- **前端**：React + TypeScript，建设可复用组件、图表与国际化资源；页面应兼顾科技感、无障碍可读性和窄屏适配。
- **桌面端**：Electron 承载同一前端，并包含受控的本地推理/缓存/同步能力。桌面端和 Web 端的权限与业务规则必须一致。
- **数据服务**：PostgreSQL 保存关系业务数据和审计信息；Milvus 首期部署并建立连接/配置/健康检查，但仅为第二阶段 RAG 留出向量集合、元数据和访问接口；Redis 用于异步任务、任务状态和短期缓存。
- **文件存储**：首期存储于本地服务器磁盘；目录、容量告警、访问权限、文件校验与删除策略必须可配置。
- **部署方式**：Windows 本地服务器。以 Docker Compose 编排 PostgreSQL、Milvus、Redis 等依赖服务；应用使用 Conda 环境运行。应提供 Windows 一键启动、停止、健康检查和初始化说明。
- **环境管理**：使用 Conda 统一管理。仓库必须包含 `environment.yml`，锁定 Python 与核心依赖版本；所有开发、测试、训练和运行命令均以该环境为前提。
- **安全**：严禁把密码、API Key、模型服务密钥写入仓库或日志。敏感字段加密保存；使用环境变量/受控配置注入；API 进行身份认证、授权、输入校验、文件白名单和访问隔离。

## 6. 非功能需求与验收基线

- 在 RTX 4060 笔记本上完成单路 OBS 虚拟摄像头演示；用户可动态调整 FPS/抽帧、分辨率或推理参数来保持可用体验。
- 所有耗时操作（视频、实时分析、LLM、训练、评估、报告）必须异步执行，提供状态、进度、取消、失败原因和重试能力，不能阻塞 Web 请求。
- 业务关键操作具有可靠事务和审计；媒体文件与数据库记录保持可追溯关联。
- LLM 调用、模型推理、媒体上传、训练任务均需有超时、异常隔离和可读错误信息。
- 前后端 API 应有 OpenAPI 文档、版本化策略与统一错误格式；核心服务需单元测试，关键流程需集成测试，图片/视频/实时/离线同步需有端到端验收用例。
- 系统界面遵循深色工业科技视觉：明确风险色、可读字号、状态不只依赖颜色、图表可筛选且可导出。不得为了视觉效果隐藏关键风险信息。

## 7. 构建阶段与 GitHub 里程碑

GitHub 目标仓库：`https://github.com/LuminousRebirth/InfraSentinel-AI.git`。

每个大型模块完成后，必须先完成代码审查、测试、文档与 `AGENT.md` 更新，再提交并推送 GitHub。使用递增里程碑标签：`v1.0`、`v1.1`、`v1.2`……；标签说明必须包含已交付模块、迁移/部署影响、测试结果和已知限制。

建议里程碑如下（具体标签可随实现调整，但顺序不应跳过依赖）：

| 版本 | 交付内容 |
|---|---|
| v1.0 | 工程基线、Conda/Docker、PostgreSQL/Milvus/Redis、配置、安全基础、AGENT.md |
| v1.1 | 注册登录、管理员审批、权限、国际化、审计基础 |
| v1.2 | YOLO26 图片/视频/单路 OBS 检测、任务队列、历史记录 |
| v1.3 | LLM 适配、多点分析、规则分级、预警中心与证据链 |
| v1.4 | 数据集管理、在线标注、质量检查、YOLO26 训练/评估/发布 |
| v1.5 | 工作台、报告、项目/点位、健康监控、成本控制、系统设置 |
| v1.6 | Electron 桌面端、离线检测、同步、完整回归与部署验收 |

## 8. Codex 执行规则

- 开始实现前先读取并更新 `AGENT.md`，不得假设上一会话状态。
- 每完成一个可验证模块：运行对应测试、记录验证证据、更新需求偏差与下一步，再进行 GitHub 里程碑提交。
- 不得擅自删除用户已有数据集、权重、训练产物或未关联的改动；不得提交任何密钥、`.env`、大模型权重、视频或数据集。
- 对数据库 schema、Docker 服务、模型训练默认参数、依赖新增、对外接口变更等重要决策，必须先更新本文件或相应设计文档。
- 第一版的 Agent/RAG 只做技术预留，未经明确新需求不得提前实现业务问答功能。

## 9. 完成定义

第一版完成的条件：用户可在 Web 和桌面端使用经管理员审核的账号，选择或上传检测媒体，使用已发布 YOLO26 模型完成检测，按需自动/手动获取多点 LLM 分析，生成并闭环处置预警，导出报告；管理员可管理用户、项目点位、数据集、标注、训练、评估、模型发布、规则、LLM/API 与系统健康。系统可在当前 Windows + RTX 4060 笔记本环境中完整部署与验收，所有大型模块均有 GitHub 版本里程碑和可持续更新的 `AGENT.md`。

## 10. 2026-09-01 补充确认

- 当前目录初始化为全新项目，仅迁移旧 Demo 的可复用源码、测试与非敏感配置；旧仓库及其 Git 历史保持不动。
- 数据集、权重、TensorRT/ONNX 文件、视频和训练产物仅作为本地验收资产，不纳入 Git。
- 联网桌面端使用完整平台能力；离线仅保证缓存登录、图片/视频/OBS 检测、本地记录、预警暂存和恢复同步。
- 第一版按 Windows 11 + Docker Desktop/WSL2 + Windows Conda 应用进程验收，面向同一局域网访问，不包含公网发布和生产 TLS。
- 普通用户仅访问获分配项目/点位内且与本人相关的数据；管理员访问全量数据。首位管理员由环境变量驱动的初始化命令创建。
- 管道六类缺陷初始均为中风险，`no_helmet` 初始为高风险；规则可由管理员调整。实时 LLM 针对合并事件调用，不逐帧调用。
- 每张图片或每个合并视频事件采用一次结构化多模态请求，返回逐对象与全局分析。云端 API 凭据由用户后续提供，缺失时不得阻断 YOLO 与规则预警。
- 离线记录使用本地 UUID 与幂等同步；服务器配置具有最终权威，业务冲突保留双方并要求人工选择。
- 删除默认采用管理员确认、30 天隔离后物理删除并全程审计。模型灰度首期按项目/点位实施。
- 容量基线为 20 个在线用户、1 路实时流、2 个并行 GPU 推理任务、单图 50 MB、单视频 5 GB/2 小时和 1 TB 媒体盘告警基线。
- RTX 4060 实时验收基线为 640p/720p 不低于 15 FPS、端到端延迟中位数不超过 300 ms、持续 1 小时无积压或崩溃。
- Electron 第一版交付 Windows x64 安装包，不要求代码签名与自动更新；未签名包允许出现 SmartScreen 提示。
